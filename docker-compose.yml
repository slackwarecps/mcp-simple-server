services:
  mcp-server:
    build: .
    container_name: mcp-simple-server
    restart: unless-stopped
    ports:
      - "3030:3030"
    environment:
      - PORT=3030
      - AUTH_TOKEN=senha-facil-123
    networks:
      - lab_network

networks:
  lab_network:
    external: true
